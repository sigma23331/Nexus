services:
  backend:
    build: ./backend
    container_name: mind-island-backend
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build: ./frontend
    container_name: mind-island-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
