version: '3.8'

services:
  # 数据库服务
  db:
    image: postgres:15-alpine
    container_name: mind-island-db
    environment:
      POSTGRES_USER: ${DB_USER:-nexus_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-nexus_password123}
      POSTGRES_DB: ${DB_NAME:-nexus_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
<<<<<<< Updated upstream
      test: ["CMD", "pg_isready", "-U", "nexus_user", "-d", "nexus_db"]
      interval: 10s
=======
      test: ["CMD-SHELL", "pg_isready -U nexus_user -d nexus_db"]
      interval: 5s
>>>>>>> Stashed changes
      timeout: 5s
      retries: 5

  # 后端服务 (Flask)
  backend:
    build: ./backend
    container_name: mind-island-backend
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-nexus_user}:${DB_PASSWORD:-nexus_password123}@db:5432/${DB_NAME:-nexus_db}
      - FLASK_ENV=development
      - SECRET_KEY=${SECRET_KEY:-dev_secret_key}
    depends_on:
      db:
        condition: service_healthy
    # 如果本地开发需要热更新，可以取消下面注释
    # volumes:
    #   - ./backend:/app

  # 前端服务 (Vue 3 + Nginx)
  frontend:
    build: ./frontend
    container_name: mind-island-frontend
    ports:
<<<<<<< Updated upstream
      - "5000:5000"
    volumes:
      - .:/app
    command: >
      sh -c "flask db upgrade &&
             flask run --host=0.0.0.0 --port=5000"
=======
      - "80:80"
    depends_on:
      - backend
>>>>>>> Stashed changes

volumes:
  postgres_data: